# outputs as the stored snapshot

    Code
      cat(bullets("a"))
    Error <simpleError>
      could not find function "bullets"

---

    Code
      cat(bullets("a", "b", "c"))
    Error <simpleError>
      could not find function "bullets"

