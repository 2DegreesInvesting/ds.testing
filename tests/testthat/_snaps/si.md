# outputs the saved snapshot

    Code
      si()
    Message <simpleMessage>
      * Copy from console:
    Output
      <details>
      ```{r}
      devtools::session_info()
      ```
      </details>

---

    Code
      si("bad")
    Error <simpleError>
      unused argument ("bad")

